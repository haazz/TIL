# 1. "=="

- primitive type 에서 값 자체를 비교
- wrapper class 에서는 참조값(주소)을 비교

# 2. ".equals()"

- Overriding 하지 않은 상태에서는 기본적으로 wrapper class 에서는 참조값(주소)을 비교
- 클래스에서 Object 의 .equals()를 Overriding을 하면 객체 내부의 실제 값이 같은지를 비교
