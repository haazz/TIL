finalize()는 GC가 객체를 제거하기 전에 호출될 수 있도록 설계된 메소드이다. 하지만 Java 9 버전부터 Deprecated 됐다.

# Deprecated 이유

1. 호출 시점이 전혀 예측되지 않는다. (프로그램 종료 시에도 호출되지 않을 수 있다.)
2. GC과정이 복잡해져 성능 저하의 원인이 될 수 있다.
3. finalize() 내부에서 객체가 호출되면 객체가 부활하여 메모리 누수가 발생할 수 있다.
4. finalize() 내부에서 예외가 발생하면 로그 없이 무시가 된는 문제가 발생한다.
