# 메소드 영역

- 전역변수와 static변수가 저장된다.
- JVM이 동작해서 클래스가 로딩될 때 생성되고 프로그램 종료까지 메모리에 남아있다.

# 스택 영역

- 지역변수와 매개변수 데이터 값이 저장된다.
- 메소드가 호출될 때 메모리에 할당되고 종료되면 메모리가 해제된다.

# 힙 영역

- new 키워드로 생성되는 객체 (인스턴스), 배열 등이 정장된다.
- 런타임 시 동적으로 할당되며, GC에 의해 관리된다.
